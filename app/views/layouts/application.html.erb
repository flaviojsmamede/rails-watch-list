<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <nav class="navbar-cinema container">
      <div class="brand d-flex align-items-center">
        <i class="fa-solid fa-film"></i>
        <strong><a href="/lists">CINELIST</a></strong>
      </div>
      <%# <div class="p-2"> %>
        <%# <%= link_to "My lists", lists_path, class: "link-secondary link-offset-2 link-underline link-underline-opacity-0 m-2"%>
        <%# <%= link_to "New List", new_list_path, class: "link-secondary link-offset-2 link-underline link-underline-opacity-0 m-2" %>
      <%# </div> %>
      <%= link_to "Create a movie list", new_list_path, class: "btn btn-outline-dark" %>
    </nav>

    <div class="banner">
      <div class="container">
        <h1>Save any kind of <strong>movies</strong></h1>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
